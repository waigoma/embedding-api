# Image source
# ghcr.io/<owner>/<repo>-nvidia:<tag>
# ghcr.io/<owner>/<repo>-amd:<tag>
EMBEDDING_IMAGE=ghcr.io/<owner>/<repo>-amd:latest

# Device mode: auto|cpu|gpu|cuda
DEVICE_MODE=auto

# Optional Hugging Face token (for private/gated repos)
HF_TOKEN=
